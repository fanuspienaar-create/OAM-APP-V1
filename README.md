OAM Studio v17 — Quote/Invoice workflow update.

New: Quote → Invoice conversion; invoice numbering follows the highest existing invoice number; quotes can be closed or deleted if created in error; converted quotes remain saved and show their linked invoice. Approved PDF layout retained.
